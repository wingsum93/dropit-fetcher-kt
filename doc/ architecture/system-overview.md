# system-overview.md
the system is scrape data from www.dropit.dm (simple ecommerce website), consist of control UI (with api) called server, and worker node (kt)
worker node will operate in db instead of api to pass data.